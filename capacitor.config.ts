import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sirahlabs.ls2d',
  appName: 'Village Life Simulator 2d',
  webDir: 'dist'
};

export default config;
